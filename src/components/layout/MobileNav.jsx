/**
 * Mobile navigation affordances are handled by Header + Sidebar drawer.
 * Kept as a dedicated module for extendability (tabs, bottom nav, etc.).
 */
export default function MobileNav() {
  return null
}
